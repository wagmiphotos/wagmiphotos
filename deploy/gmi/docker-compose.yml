# One-box GMI deployment: backfill worker (in-process BGE).
# Secrets/config come from deploy/gmi/.env (git-ignored); see README.md here.
services:
  backfill:
    build:
      context: ../..
      dockerfile: projects/backfill/Dockerfile
    env_file:
      - .env
    restart: unless-stopped
