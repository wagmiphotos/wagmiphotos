# Handoff — 2026-07-07

Resume guide for the session that shipped **magic-link auth** and the **BGE edge
embeddings** swap. Everything below is on `main`, green, and **pushed to
`github.com/wagmiphotos/wagmiphotos`** (the repo was renamed from `sharedcache`;
`origin` is repointed to the new URL).

## State

- Branch: `main` @ `a17f00c`. Tests green: **Worker 113 / Python 46**.
  - Worker: `cd projects/worker && npx vitest run`
  - Python: `uv run pytest -q`
- **Nothing has touched live Cloudflare/GMI infra yet** — it's all code-complete + offline-verified.
- Full task-by-task trail: `.superpowers/sdd/progress.md` (git-ignored). Design docs:
  `docs/superpowers/specs/2026-07-06-*.md`, plans `docs/superpowers/plans/2026-07-06-*.md`.

## Shipped this session (all merged to main + reviewed)

1. **Landing reframe + FAQ** — hero is "Free, open images / Shared by everyone"; accurate
   closest-match + background-backfill copy; removed the misleading "Retrieval vs fresh
   generation" section; 7-item FAQ.
2. **Accounts + magic-link login** (spec/plan `2026-07-06-accounts-magic-link-auth`) —
   passwordless email login (Resend) gating playground/library/account + the API. Two lanes:
   HttpOnly `wagmi_session` cookie (humans) + user-owned Bearer keys (SDK); `MASTER_API_KEY`
   admin; dev-open API when master unset. **Nonce-bound tokens** (`wagmi_login` cookie)
   close login session-fixation + email-scanner prefetch. D1 migrations **0004** (users/
   sessions/login_tokens + `api_keys.user_id`) and **0005** (`login_tokens.nonce_hash`).
3. **Account UI** — nav avatar dropdown (Account/Log out); "Create API key" modal (name →
   generate → shown-once + copy + warning); **delete key** (owner-scoped); dropped the dead
   HF-token field; playground now uses the session cookie.
4. **BGE edge embeddings** (spec/plan `2026-07-06-bge-edge-embeddings`) — replaced CLIP
   cross-modal with **BGE `bge-base-en-v1.5` text-to-text (prompt-to-prompt)**. Worker embeds
   queries via **Workers AI** `@cf/baai/bge-base-en-v1.5` (`[ai]` binding); backfill embeds
   prompts with **local** `BAAI/bge-base-en-v1.5` (opt-in `[model]` extra; base torch-free).
   New index **`wagmiphotos-bge`**; CLIP embedder retired (`projects/embedder` deleted).
5. **GitHub repo → wagmiphotos**; `origin` remote repointed; `main` pushed.

## Pick up tomorrow (priority order)

### 1. Deploy — BGE live provisioning (Task 6) — biggest open item
Needs real Cloudflare + GMI credentials. Runbook: `DEPLOY.md` (esp. §2, §6). Sequence:
- Create the index: `npx wrangler vectorize create wagmiphotos-bge --dimensions=768 --metric=cosine`.
- **Drift check (required):** embed a ~10-string fixture with the Worker's Workers-AI BGE
  AND `sharedcache.common.bge.BgeEmbedder.from_pretrained().text_embed(...)`; assert cosine
  **≥ 0.98**. This is the only check code review can't do (Workers-AI vs sentence-transformers
  pooling equivalence). If it fails → reconcile prefix/pooling/normalize.
- Seed: `uv run python -m sharedcache.backfill.seed_pd12m --limit 100`.
- Deploy the Worker (`npm run deploy`), then **tune** `FLOOR_SIM_MAX`/`FLOOR_SIM_MIN` against
  the seeded pool (`0.90/0.72` are guesses; BGE cosines run ~0.7–0.95).
- Do a real `docker build` of the backfill image with `--extra model` (only `--dry-run`'d so far).
- Also: the accounts deploy — apply migrations 0004+0005 remotely BEFORE `npm run deploy`;
  `wrangler secret put RESEND_API_KEY`; verify the `EMAIL_FROM` sending domain in Resend.

### 2. `sharedcache → wagmiphotos` rename (separate task, brainstorm→spec→plan→SDD)
Only NEW artifacts use the wagmiphotos name so far (index, git remote, star badge). Still
`sharedcache`: D1 database name (`sharedcache`), worker name (`sharedcache-worker`), the
Python namespace + packages (`sharedcache.*`, `sharedcache-common/generation/backfill`), and
various docs. The D1 rename is a migration; the namespace touches every Python import — real work.

### 3. Deferred minors (triage before real traffic)
- Auth: no GC of expired `login_tokens`/`sessions` (unbounded growth) — add a cleanup.
- BGE: `floor.ts`/`floor.py` bare **function-signature** defaults were swept to 0.90/0.72;
  a low-sev rate-limited-vs-accepted response-header oracle on `/v1/auth/login` (dev-gated).
- A miniflare real-D1 integration harness (retires the "fakeDb only checks SQL text" blind spot).
- Cross-device magic links would need an OTP path (currently same-browser only, by nonce design).

## Gotchas
- **Local dev auth:** `RESEND_API_KEY` unset → magic link returned as `dev_link`; you MUST
  `--var PUBLIC_SITE_URL:http://127.0.0.1:8787` or the link points at prod. See the
  `running-locally` skill (`.claude/skills/running-locally/SKILL.md`).
- **Generation still 502s locally** — Vectorize has no local emulation and Workers AI needs a
  real account; the offline path is playground/library/keys, not real matching/generation.
- `main` is pushed but the `wagmiphotos/wagmiphotos` repo may **move again** to another new repo (per user).
