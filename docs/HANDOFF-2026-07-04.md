# Session handoff — 2026-07-04

Where things stand and what to do next. For architecture / how-to-run, see the
root `HANDOFF.md`. For the full task-by-task audit trail, see
`.superpowers/sdd/progress.md`.

## TL;DR

Three features shipped and a round of deferred-hardening items closed. **All
pushed to `origin/main`.** The next real step is the first live deploy —
follow **`DEPLOY.md`** (needs Cloudflare / B2 / GMI credentials).

- Suites green on `main`: **Python 58/58**, **Worker 75/75**.
- Working tree clean (only untracked `.playwright-mcp/` scratch — ignore).
- Deferred-hardening progress is in "Addressed since" below.

## What shipped

### 1. Library page (`#/library`)
Public keyword search + download over the shared image library.
- `GET /v1/library?q=&limit=&offset=` — D1 `LIKE` over `assets.prompt`,
  whitespace-tokenized AND terms, `has_more` pagination. Migration
  `0003_assets_browse_index.sql` backs the browse ordering.
- `GET /v1/library/:id/download` — CORS-safe worker proxy, attachment filename
  from prompt slug; 404 on unknown/malformed id.
- `src/library.ts` (handlers), routed in `src/index.ts`; `#/library` SPA view
  (debounced search, responsive grid, lazy thumbs, load-more).
- Also fixed en route: playground/docs no longer reference the retired
  `shared-cache-*` "target model" — the API is `prompt` + `cache_tolerance` +
  `generate_on_miss`, and the playground now has a generate-on-demand toggle.

### 2. Configurable public URLs
- `wrangler.toml` vars `PUBLIC_SITE_URL` / `PUBLIC_API_BASE_URL`
  (`wagmi.photos` / `api.wagmi.photos/v1`). `src/rewrite.ts` substitutes them
  into served HTML at request time, so a dev deployment renders its own URLs.
  SPA fetches stay origin-relative (no CORS). Verified E2E with a `.dev.vars`
  override on a second wrangler instance.

### 3. GMI embedder + backfill deployment
The missing CLIP endpoint + a production home for the backfill.
- `projects/embedder/` — FastAPI CLIP ViT-L/14 service; protocol matches the
  existing Worker (`src/embed.ts`) and Python (`common/clip.py`) clients;
  tests offline/torch-free (model deps behind a `[model]` extra).
- `deploy/gmi/` — compose stack: `embedder` + `backfill` + `cloudflared`
  tunnel publishing `embed.wagmi.photos`. Image builds with weights baked in;
  smoke-tested serving real 768-dim vectors.
- Hardened per review: **fail-closed auth** (container refuses to boot without
  `EMBED_TOKEN` — it's public via the tunnel), pinned model deps
  (torch 2.12.1 / torchvision 0.27.1 / open-clip 3.3.0 / pillow 12.2.0) +
  `cloudflared:2026.6.1`, inference moved off the event loop, 10 MB body cap.

## Next steps (prioritized)

1. ~~Push `main` to `origin`~~ — **done**, `origin/main` is current.
2. **First live deploy — follow `DEPLOY.md`.** It's the ordered runbook: auth →
   **create D1 (doesn't exist yet — `wrangler.toml` id is still a placeholder)
   + wire its id** → create Vectorize → apply migrations remotely (before any
   deploy) → GMI box → `CLIP_EMBED_TOKEN` secret → route domains + `npm run
   deploy` → seed + tune `FLOOR_SIM_*`. Ask Claude to make the `database_id`
   edit once you paste the id from `wrangler d1 create`.
3. **Confirm PD12M exposes precomputed CLIP image vectors** (open question from
   the original edge-cache spec) — if not, the seeder embeds captions instead.
   Settle this during seeding.

## Addressed since (all pushed to `origin/main`)

Worker (`fc9b7a9`, `a691854`):
- Library search projects to the documented public shape — no more
  `source_id`/`source_url`/`locally_cached` leak.
- `q` capped at 200 chars (`400` over the cap).
- Master API-key compare is constant-time over fixed-length SHA-256 digests.
- `POST /v1/images/generations` is rate-limited per client IP before the
  CLIP+Vectorize path (was: only keygen throttled).
- `numEnv()` validates numeric env (`FLOOR_SIM_*`, `IMAGE_PRICE_USD`): blank/
  unparseable → default, a genuine `0` honored, no stray `NaN` downstream.
- Non-GET on the library download route covered (404).

Backfill / embedder (`cc9bbc4`) + deploy (`e6bc548`):
- Rehost streams with a 25 MB cap (OOM defense).
- Optional lifetime spend cap (`worker_max_lifetime_spend_usd`) across ticks.
- Generated assets write `assets/<id>/manifest.json` provenance to B2 and set
  `manifest_url` (spec §4.3).
- `build_worker_from_settings` warns on StubGenerator fallback.
- Embedder: valid-but-non-dict JSON body → 422 covered.
- Backfill + embedder containers run as non-root (uid 10001).

## Deferred minors (none blocking; tracked in the SDD ledger)

- Library: no edge caching on the library endpoints.
- Backfill: rehost has a size cap but no host allowlist (add when `source_url`
  can be user-influenced); non-root Dockerfiles pass `--check` but want a real
  `docker compose up --build` smoke test.
- Embedder: `TestClient` emits a third-party `StarletteDeprecationWarning`.
- Follow-up: a real-D1 (miniflare) integration harness would retire the
  "fakeDb records SQL but never executes it" blind spot; prompt→embedding
  caching in the Worker would cut most CLIP calls off the hot path.

## Design trail (this session)

- `docs/superpowers/specs/2026-07-03-library-page-design.md` (+ plan)
- `docs/superpowers/specs/2026-07-03-public-url-config-design.md` (+ plan)
- `docs/superpowers/specs/2026-07-03-gmi-embedder-deployment-design.md` (+ plan)
